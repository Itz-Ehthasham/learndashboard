MONGODB_URI=mongodb://localhost:27017/learning-analytics
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRE=7d
PORT=5000
# Comma-separated if you use multiple dev URLs (e.g. localhost and 127.0.0.1)
FRONTEND_URL=http://localhost:3000
# Optional: default admin created on server start if missing (login with email or username Admin)
# DEFAULT_ADMIN_EMAIL=admin@admin.com
# DEFAULT_ADMIN_PASSWORD=Admin123
